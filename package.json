{
  "name": "portfolio",
  "private": true,
  "version": "0.1.0",
  "description": "Portfolio pessoal full stack com identidade visual inspirada em kaiju/Godzilla",
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "dev": "concurrently -n web,api -c blue,green \"pnpm --filter web dev\" \"pnpm --filter api dev\"",
    "dev:web": "pnpm --filter web dev",
    "dev:api": "pnpm --filter api dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "test:watch": "concurrently -n api,web -c green,blue \"pnpm --filter api test:watch\" \"pnpm --filter web test:watch\"",
    "test:coverage": "pnpm -r --if-present test:coverage",
    "test:e2e": "pnpm --filter web test:e2e",
    "typecheck": "turbo run typecheck",
    "storybook": "pnpm --filter storybook storybook",
    "storybook:build": "turbo run build --filter=storybook",
    "playground": "pnpm --filter playground dev",
    "format": "prettier --write .",
    "clean": "pnpm -r exec rimraf dist .next .turbo storybook-static node_modules",
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down",
    "seed": "pnpm --filter api seed"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "prettier": "^3.4.2",
    "rimraf": "^6.0.1",
    "turbo": "^2.3.3",
    "typescript": "^6.0.3"
  }
}
