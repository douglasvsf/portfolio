{
  "name": "@godzilla/tokens",
  "version": "0.1.0",
  "private": true,
  "description": "Design tokens (cores, tipografia, espaçamento, etc.) — fonte única de verdade visual do Design System",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./theme.css": "./src/theme.css"
  },
  "scripts": {
    "build": "tsc --noEmit -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "eslint ."
  },
  "devDependencies": {
    "@godzilla/eslint-config": "workspace:*",
    "@godzilla/typescript-config": "workspace:*",
    "eslint": "^9.18.0",
    "typescript": "^6.0.3"
  }
}
